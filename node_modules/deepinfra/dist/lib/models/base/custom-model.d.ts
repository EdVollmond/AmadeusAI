import { BaseModel } from "../../../lib/models/base/base-model";
import { IClientConfig } from "../../../lib/types/common/client-config";
export declare abstract class CustomModel<Request extends object, Response extends object> extends BaseModel {
    protected constructor(modelName: string, authToken?: string, config?: Partial<IClientConfig>);
    generate(body: Request): Promise<Response>;
}
