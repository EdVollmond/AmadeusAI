import { BaseModel } from "../../../lib/models/base";
import { EmbeddingsRequest } from "../../../lib/types/embeddings/request";
import { EmbeddingsResponse } from "../../../lib/types/embeddings/response";
import { IClientConfig } from "../../../lib/types/common/client-config";
export declare class Embeddings extends BaseModel {
    constructor(modelName: string, authToken?: string, config?: Partial<IClientConfig>);
    generate(body: EmbeddingsRequest): Promise<EmbeddingsResponse>;
}
