import { BaseModel } from "../../../lib/models/base/base-model";
import { IClientConfig } from "../../../lib/types/common/client-config";
import { ImageRequest } from "../../../lib/types/common/image-request";
export declare class ImageBaseModel<RequestType extends ImageRequest, ResponseType> extends BaseModel {
    constructor(modelName: string, authToken?: string, config?: Partial<IClientConfig>);
    generate(body: RequestType): Promise<ResponseType>;
}
