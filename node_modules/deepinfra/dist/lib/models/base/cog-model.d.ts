import { BaseModel } from "../../../lib/models/base";
import { CogResponse } from "../../../lib/types/cog/response";
import { CogRequest } from "../../../lib/types/cog/request";
import { IClientConfig } from "../../../lib/types/common/client-config";
export declare class CogBaseModel<CogIn, CogOut> extends BaseModel {
    protected endpoint: string;
    constructor(endpoint: string, authToken?: string, config?: Partial<IClientConfig>);
    generate(body: CogRequest<CogIn>): Promise<CogResponse<CogIn, CogOut>>;
    private handleError;
}
