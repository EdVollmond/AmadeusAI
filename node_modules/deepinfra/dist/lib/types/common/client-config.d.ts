export interface IClientConfig {
    maxRetries: number;
    initialBackoff: number;
    subsequentBackoff: number;
}
export declare class ClientConfig implements IClientConfig {
    maxRetries: number;
    initialBackoff: number;
    subsequentBackoff: number;
    constructor(config?: Partial<IClientConfig>);
}
